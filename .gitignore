node_modules
dist
.turbo
.env
.env.local
# Every real-value env file, not just the two that were listed. A
# .env.production holds live provider keys and a signing secret; it was one
# `git add -A` away from GitHub.
.env.production
.env.production.local
.env.*.local
*.env.production
.vercel
coverage
*.tsbuildinfo
.expo/

# Go
/altar
/bin/
*.test
*.out
test-output.txt

# Seeded development logins, rewritten by `make seed`. Never real secrets,
# but a file with this name attracts them, so it stays out of git.
credentials.txt

# Raven tooling scratch. Timestamped event stubs written on every compaction —
# transient state, not project history.
.raven/.cache/
